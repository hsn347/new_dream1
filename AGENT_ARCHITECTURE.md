# 🤖 معمارية الوكيل — شرح كامل

## الملفات المسؤولة عن سلوك الوكيل

| الملف | الدور |
|-------|-------|
| `artifacts/api-server/src/routes/webhook.ts` | **المحرك الرئيسي** — يستقبل كل رسائل واتساب ويُنسّق بين جميع المكونات |
| `artifacts/api-server/src/lib/agentContext.ts` | بناء السياق النصي الذي يُحقن في الـ system prompt |
| `artifacts/api-server/src/lib/providers/groq.ts` | تعريف الأدوات (tools) + بناء الـ system prompt الكامل + الاتصال بـ Groq |
| `artifacts/api-server/src/lib/providers/gemini.ts` | الاتصال بـ Gemini (نفس الدور بديلاً عن Groq) |
| `artifacts/api-server/src/lib/providers/cohere.ts` | توليد vector embeddings للبحث الدلالي |
| `artifacts/api-server/src/lib/vectorSearch.ts` | البحث الدلالي في قاعدة المعرفة (RAG) |
| `artifacts/api-server/src/lib/orderActions.ts` | معالجة الطلبات — إنشاء/تحديث/قفل + إشعارات المراجعة |
| `artifacts/api-server/src/lib/returnActions.ts` | معالجة طلبات الاسترجاع |
| `artifacts/api-server/src/lib/omqi.ts` | التحقق من سندات الإيداع (Omqi) وقبول الطلبات تلقائياً |
| `artifacts/api-server/src/lib/invoice.ts` | توليد فاتورة PDF وإرسالها للعميل عبر واتساب |
| `artifacts/api-server/src/lib/profileExtractor.ts` | تحديث بيانات العميل تلقائياً من المحادثة |
| `artifacts/api-server/src/lib/reports.ts` | التقارير الدورية المجدولة + متابعة السلال المهجورة |
| `artifacts/api-server/src/lib/providers/evolution.ts` | إرسال الرسائل/الصور/المستندات عبر Evolution API |

---

## 🔄 رحلة الرسالة من البداية إلى النهاية

```
[عميل يكتب على واتساب]
         ↓
[Evolution API ← Webhook POST /api/webhooks/evolution]
         ↓
[1. تحديد نوع الحدث: رسالة نصية / صوتية / صورة / كتابة (composing)]
         ↓
[2. فحص التكرار (deduplication) — تجاهل نفس الرسالة خلال 60 ثانية]
         ↓
[3. Buffer — تجميع الرسائل المتتالية]
    → رسالة جديدة: أضف للـ buffer + أعد ضبط المؤقت (6 ثانية)
    → يكتب الآن (composing): مدّد المؤقت (15 ثانية)
    → حد أقصى: 120 ثانية
         ↓
[4. processTextFlow — المعالجة الكاملة]
         ↓
[5. فحوصات أولية]
    → المستخدم active؟
    → الوكيل مفعّل (agentEnabled)؟
    → إعدادات واتساب موجودة؟
    → المحادثة مُوقفة (agentPaused)؟
         ↓
[6. بناء السياق الكامل]
    → تحميل الإعدادات العالمية (admin) + إعدادات المستخدم
    → جلب سجل المحادثة (آخر N رسالة — قابل للضبط)
    → فحص حدود الجلسة (sessionGapHours — افتراضي 6 ساعات)
    → جلب الطلب النشط إن وجد
    → فحص نية الكوبون → جلب الكوبونات الفعالة
    → البحث الدلالي (Vector Search) إن كان مفتاح embedding متاحاً
         ↓
[7. بناء الـ system prompt الكامل]
         ↓
[8. كشف مباشر — بدون LLM]
    → طلب كتالوج؟ → رد جاهز بقائمة المنتجات مباشرة
         ↓
[9. إرسال طلب للـ LLM مع Multi-key Failover]
    → المفتاح الأساسي أولاً → Fallbacks → أي مفتاح نشط
    → Groq: محاولة 3 مرات مع exponential backoff
    → على فشل مفتاح: إشعار الأدمن عبر واتساب
         ↓
[10. معالجة الرد]
    → Tool calls → معالجة submit_order / request_return
    → إن استدعى أداة بدون نص: LLM call ثانية بدون أدوات
    → تنظيف الرد (إزالة JSON / code blocks / تسرب system prompt)
         ↓
[11. إرسال الرد للعميل]
    → typing indicator أثناء التفكير
    → تأخير اختياري (responseDelay)
    → إرسال صورة منتج تلقائياً إن وُجدت
         ↓
[12. مهام ما بعد الرد]
    → حفظ الرسالة في DB
    → تحديث customer profile (rule-based — بدون LLM)
    → تحديث إحصائيات استخدام المفتاح
```

---

## 🧠 كيف يبني الوكيل عقله (System Prompt)

### الطبقة الأولى: الهوية والشخصية (groq.ts → `buildStrictSystemPrompt`)

هذا هو قلب الوكيل. يُبنى من الإعدادات التالية:

| الإعداد | القيم الممكنة | الافتراضي |
|---------|--------------|----------|
| `dialect` | gulf / saudi / egyptian / levantine / msa | gulf |
| `dialectStrength` | 1–10 | 5 |
| `style` | friendly / formal / casual / salesperson | friendly |
| `tone` | warm / professional / energetic / calm | warm |
| `persuasion` | 1–10 | 7 |
| `formality` | 1–10 | 5 |
| `emojiLevel` | none / low / medium / high | medium |
| `replyLength` | short / medium / long | medium |

**أولوية الإعدادات:**
- إذا `adminOverride = true` → إعدادات الأدمن تفوق إعدادات المستخدم
- إذا `adminOverride = false` → إعدادات المستخدم تفوز، إعدادات الأدمن كـ defaults

### الطبقة الثانية: قواعد صارمة مضمّنة في الـ prompt

- **تحية فقط؟** → رحّب بجملة واحدة فقط، لا تذكر منتجاً
- **الاسم** → استخدم الاسم الأول فقط دائماً
- **لا تكرر الأسئلة** → ابحث في سجل المحادثة قبل أي سؤال
- **لا تخترع** → لا أسعار ولا منتجات غير موجودة
- **الكوبونات** → لا تذكرها إلا إذا طلب العميل صراحةً
- **التفاوض** → ابدأ بالسعر الرسمي، قدّم خصماً فقط عند مقاومة السعر

### الطبقة الثالثة: استراتيجيات البيع (قابلة للتشغيل/الإيقاف)

| الاستراتيجية | الوظيفة |
|-------------|---------|
| `stratUpsell` | اقتراح منتج مكمل عند الاستفسار |
| `stratCart` | تذكير العميل بما توقف عنده |
| `stratFollowup` | متابعة الطلبات المعلقة بعد فترة |
| `stratPromo` | ذكر الكوبونات عند قرب الشراء فقط |
| `stratReview` | شكر العميل بعد تأكيد الإيداع |

---

## 📦 من أين يحصل الوكيل على بياناته؟

### بيانات مُحقنة مباشرة في كل طلب (agentContext.ts)
- اسم النشاط التجاري + وصفه
- سياسة الاسترجاع والاستبدال
- تكاليف التوصيل حسب المناطق
- الطلب النشط الحالي (draft/pending_payment/pending_review)
- الكوبونات الفعالة (فقط إذا الرسالة تحتوي نية كوبون)

### بيانات عبر البحث الدلالي — RAG (vectorSearch.ts)
كل هذه البيانات مُحوَّلة إلى vectors ومخزنة في `knowledge_chunks`:
- **المنتجات**: اسم، وصف، سعر، كمية، حالة
- **الكوبونات**: كود، نوع الخصم، المنتجات المشمولة، التواريخ
- **معلومات المتجر**: وصف عام، رابط الموقع
- **التواصل والفروع**: أرقام الهاتف، أسماء الفروع
- **ساعات العمل**
- **الحسابات البنكية**: اسم البنك، IBAN، صاحب الحساب
- **روابط التواصل الاجتماعي**
- **سياسة الاسترجاع**

**آلية البحث:** عند وصول رسالة جديدة:
1. يتحول نص الرسالة إلى vector عبر Cohere
2. يُحسب cosine similarity مع كل الـ chunks المخزنة
3. أعلى 10 نتائج بدرجة ≥ 0.15 تُحقن في أول الـ context كـ "الأكثر صلة"
4. باقي بيانات المتجر تأتي بعدها كمرجع شامل

### بيانات الجلسة (webhook.ts)
- آخر N رسالة من `messages` table (N = convWindow، افتراضي 6)
- إذا مرّ أكثر من sessionGapHours (افتراضي 6 ساعات) منذ آخر رسالة → جلسة جديدة

---

## 🛠️ الأدوات (Function Calling Tools)

يمتلك الوكيل أداتين رسميتين يستدعيهما عند الحاجة:

### `submit_order` — حفظ الطلب
**يُستدعى عند:** اكتمال معلومات الطلب (اسم + عنوان + منتجات + إجمالي)

يُرسل هذه البيانات:
```
customerName, customerPhone, customerAddress
items: [{name, qty, unit, price, total}]
subtotal, deliveryCost, total, currency
depositReference (اختياري — رقم مرجعي للإيداع)
```

**ما يحدث في الـ backend:**
1. إنشاء/تحديث طلب بحالة `pending_payment`
2. إذا كان يحتوي `depositReference` → الحالة تصبح `pending_review`
3. إرسال إشعار للمراجعة على رقم الواتساب المحدد
4. إنشاء تنبيه في لوحة التحكم

**قواعد صارمة على الوكيل:**
- لا يستدعي `submit_order` إذا كان هناك طلب `pending_review` نشط
- الطلب المقفل لا يُعدَّل → يُخبر العميل أن طلبه قيد المراجعة

### `request_return` — طلب استرجاع
**يُستدعى عند:** رغبة العميل في إرجاع منتج أو طلب بأكمله

يُرسل: `orderId?, customerName, customerPhone, reason, items`

**ما يحدث:** تسجيل طلب الاسترجاع في `returns` table + إشعار فوري لصاحب العمل

---

## 🖼️ نظام صور المنتجات

عند تفعيل `sendProductImages`:
1. يبحث في اسم المنتجات داخل رسالة العميل
2. إذا وجد منتجاً مطابقاً بصورة ولم تُرسَل من قبل → يُعد للإرسال
3. إذا طلب العميل صريحاً "صورة" → يُرسل صورة أول منتج متاح
4. بعد إرسال الرد النصي بـ 800ms → يُرسل الصورة
5. يتذكر أي صور أُرسلت مسبقاً (sentImageProductIds) → لا تكرار

---

## 💳 نظام التحقق من الإيداع (Omqi)

عند وصول صورة/PDF من العميل:
1. تُحمَّل الصورة عبر Evolution API
2. تُرسل لـ Omqi للتحليل
3. Omqi يستخرج: رقم الإيصال، المبلغ، الحساب، الاسم، التاريخ
4. يُفحص إذا تم استخدام نفس رقم الإيصال من قبل (dedup)
5. يُقارن المبلغ بإجمالي الطلب (مع هامش tolerance قابل للضبط)
6. إذا نجح التحقق → الطلب يصبح `approved` تلقائياً + إرسال فاتورة PDF

---

## 📊 التقارير الدورية (reports.ts)

جدولة تلقائية تُفحص كل 60 ثانية:

**أنواع التقارير:** يومي / أسبوعي / شهري (أو مجموعة منها)

**محتوى كل تقرير:**
- إجمالي الإيرادات + مقارنة بالفترة السابقة (↑↓%)
- عدد الطلبات وحالاتها
- عدد المحادثات والرسائل الجديدة
- أكثر 3 منتجات مبيعاً
- الطلبات المعلقة + المنتجات منخفضة المخزون
- توصيات ذكية (نصية مُولَّدة بقواعد ثابتة — بدون LLM)

**متابعة السلال المهجورة:** إذا كان هناك طلب `pending_payment` منذ أكثر من فترة محددة → رسالة تذكير للعميل

---

## 👤 تحديث الملف الشخصي للعميل (profileExtractor.ts)

بعد كل رسالة — بدون استدعاء LLM، بالكامل بقواعد:
- **الاسم**: من push name في واتساب أو من آخر طلب
- **المدينة**: من عنوان التوصيل في آخر طلب
- **isBuyer**: هل أكمل طلباً سابقاً (approved/delivered)؟
- **المنتجات المستفسَر عنها**: string match مع أسماء المنتجات في DB
- **آخر نشاط**: timestamp محدّث في كل رسالة

---

## ⚙️ الإعدادات القابلة للتخصيص

### إعدادات الأدمن (system_settings — مفاتيح agent_default_*)
تُقرأ كل 30 ثانية من DB وتُطبَّق على الكل أو كـ defaults:
- شخصية الوكيل الكاملة (لهجة، أسلوب، نبرة، إقناع...)
- استراتيجيات البيع الخمس
- الحد الأقصى للتوكنز (maxTokens) — افتراضي 1500
- نافذة المحادثة (convWindow) — افتراضي 6 رسائل
- تأخير الرد (responseDelay) — افتراضي 0 ثانية
- حد المخزون المنخفض (lowStockThreshold)
- وقت انتهاء الجلسة (sessionGapHours) — افتراضي 6 ساعات
- وضع الردود على المجموعات (groupReplyMode)
- تفعيل/إيقاف: نظام الطلبات، نظام الاسترجاع، إرسال الصور، استخراج الملف الشخصي، التفاوض

### إعدادات buffer الرسائل (system_settings)
| المفتاح | الافتراضي | الوظيفة |
|--------|----------|---------|
| `buffer_message_wait_ms` | 6000 | الانتظار بعد رسالة جديدة |
| `buffer_composing_wait_ms` | 15000 | الانتظار عند رؤية "يكتب الآن" |

### إعدادات المستخدم (user_settings)
- اختيار مفتاح LLM الأساسي + قائمة الاحتياطية (fallback chain)
- مفتاح embedding المخصص
- تفعيل/إيقاف الوكيل (agentEnabled)
- رقم واتساب المراجعة (reviewWhatsappNumber)
- الـ system prompt المخصص (يُضاف على الـ prompt العام)
- لون الفاتورة وتفعيلها (invoiceColor، invoiceEnabled)
- جدول التقارير (reportManagerPhone، reportTime، reportFrequency)

---

## 🔑 Multi-Key Failover

```
[Primary Key] → فشل؟
    ↓
[Fallback Keys (بالترتيب)] → فشل كلها؟
    ↓
[أي مفتاح chat نشط في النظام]
    ↓
إشعار الأدمن على واتساب عند كل فشل مفتاح
```

الوكيل يدعم Groq و Gemini في نفس الـ failover chain — يكتشف النوع من اسم الـ provider تلقائياً.

---

## 🔒 عزل البيانات بين المستأجرين

**كل عملية في النظام تُفلتر بـ `userId`:**
- المحادثات والرسائل → `userId`
- المنتجات والكوبونات → `userId`
- الطلبات والاسترجاعات → `userId`
- Knowledge chunks (vectors) → `userId`
- إعدادات المستخدم → `userId`

لا توجد بيانات مشتركة بين المستأجرين إلا: مفاتيح API المُدارة من الأدمن (api_keys table).
